[tool.ruff]
target-version = "py311"
line-length = 100
# The plan doc contains illustrative Python snippets — not code to lint.
extend-exclude = ["docs"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
